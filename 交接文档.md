# kkk 驱动稳定性问题交接

## 当前对象

- 仓库：`https://github.com/meoxjdn/kkk`
- 本地工作目录：`C:\Users\a\Documents\新建文件夹\kkk-repo-20260831`
- 目标设备：AArch64 Android 16，内核 `6.6.118-android15-8-g93e223c276e7-abogki500782043-4k`
- 目标 KMI：`android15-6.6`
- 客户端源码：`C:\Users\a\Downloads\client.c`
- 刷入脚本：`C:\Users\a\Downloads\刷入驱动.sh`

## 已验证事实

1. GitHub Actions 的 7 个 KMI 矩阵曾在提交 `97ed576` 上全部编译成功。
2. 设备端驱动能加载，并记录过：`ghost: netlink proto 26 registered`。
3. 客户端早期把 `sendmsg()` 成功误报为 Hook 成功；后续已加入 INSTALL ACK。
4. Android 6.6 的 `do_page_fault` ABI 是：

   ```c
   do_page_fault(unsigned long addr, unsigned int esr,
                 struct pt_regs *regs);
   ```

5. 日志中没有稳定出现 `ghost: install`、`ghost: fault` 或 `ghost: handled`，因此目前不能证明功能真正命中过。
6. `ghost-dmesg-live(1).log` 文件约 2.3 MB，但内容为全零，不能作为有效内核栈。
7. 设备确实发生过多次 `reboot`，但普通 ADB 日志没有提供可归因到 `ghost` 的 panic 栈。

## 当前代码中的高风险点

- 直接覆写内核 `do_page_fault` 文本。
- 在页故障路径中修改用户 PTE、刷 TLB，并可能进入锁/内存管理路径。
- trampoline 依赖手工搬移 AArch64 指令和绝对跳转。
- 用户功能逻辑通过 UXN 页权限制造异常，异常频率和并发不可控。
- 模块自隐身后，卸载和恢复路径不容易验证。

## 已有修复记录

- 修正 `mm` 引用释放。
- 内存读取改为动态解析 `access_process_vm`。
- 修正 Netlink payload 长度判断。
- 增加 BTI 入口兼容和 prologue 检查。
- 增加 INSTALL ACK、故障诊断日志和每 CPU 重入保护。
- 移除 `damage` 分支中无依据的 `sp -= 0x40`。

## 下一步建议

不要继续在真实设备上反复测试当前 `do_page_fault + UXN` 路线。按以下边界重构：

1. 内核模块只保留 Netlink、目标进程引用和安全内存读取。
2. 删除对 `do_page_fault` 的文本覆写和 trampoline。
3. 删除在异常上下文中修改用户页表和调度重武装 workqueue。
4. 诊断使用官方 Kprobe/Ftrace 机制，只做观测和计数，不改异常返回路径。
5. 用户态 native/Unity 函数改用成熟 inline-hook 库进行验证：
   - ShadowHook：<https://github.com/bytedance/android-inline-hook>
   - Dobby：<https://github.com/jmpews/Dobby>
   - ByteHook：<https://github.com/bytedance/bhook>
6. 保留 ACK 协议，并增加明确的 `install_rc`、`patch_state`、`hit_count` 查询。

## 公开参考

- ARM64 页故障实现：<https://github.com/torvalds/linux/blob/v6.6/arch/arm64/mm/fault.c>
- Kprobe 示例：<https://github.com/torvalds/linux/blob/v6.6/samples/kprobes/kprobe_example.c>
- Ftrace trampoline 示例：<https://github.com/torvalds/linux/blob/v6.6/samples/ftrace/ftrace-direct.c>
- Kprobe 文档：<https://docs.kernel.org/trace/kprobes.html>
- Ftrace 文档：<https://docs.kernel.org/trace/ftrace.html>

## 继续工作前的检查

- 确认本地分支与远端 `main` 同步；当前本地分支曾与远端 API 提交存在偏离。
- 使用 GitHub Actions 最新成功构建的 artifact，不要混用旧 `.ko`。
- 先验证“只加载、不打补丁”版本；用户态功能改由 ShadowHook/Dobby 等成熟库承载，内核模块不再恢复异常劫持。
- 每次实验保存客户端输出、驱动 ACK 和电脑端 ADB 监听日志。

## 本轮已完成

- 删除 `do_page_fault` 文本覆写、AArch64 trampoline、UXN/PTE 修改和重武装 workqueue。
- `INSTALL` 现在只校验参数、获取目标 `task_struct/mm_struct` 引用并记录配置。
- 增加 `CMD_STATUS`/`CMD_STATUS_ACK`，返回 `install_rc`、`patch_state`、`hit_count`、`tid` 和 `tgid`。
- `MEM_READ` 改为固定上限 4096 字节、检查地址溢出，并通过 `get_pid_task()` 管理任务引用。
- 保留可选 `do_page_fault` Kprobe 作为观测计数器；预处理函数不修改寄存器、不改变异常返回值。
- `Makefile` 纳入 `dynamic_resolver.o`；移除旧 `main.c` 的 VFS 用户指针命令路径。

## 下一步验证命令

在装有目标 Android 内核构建树的 Linux 环境执行：

```sh
make KDIR=/path/to/android/kernel clean
make KDIR=/path/to/android/kernel W=1
```

设备端先只加载模块，然后发送 `CMD_PING` 和 `CMD_STATUS`；确认收到
`PONG`、`STATUS_ACK.install_rc=-19` 且设备不重启后，再发送一个有效
`INSTALL`，最后用 `STATUS` 检查 `patch_state=1`。本轮 Windows 工作区未执行
内核构建：WSL 仅有宿主内核环境，缺少目标 Android 内核头文件（默认构建目录不存在）。
